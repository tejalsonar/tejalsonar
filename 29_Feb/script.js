//Write a JavaScript function that accepts a string as a parameter and finds the longest word within the string.
//Example string : 'Web Development Tutorial'

//write a program to find the greatest number in the array.
let arrayOfNumber = [7, 8, 4, 77, 5, 2, 1];

function largestElement(arr) {
    requestAnimationFrame.Math.max(...arr);
}

let requirdeElement = largestElement(arrayOfNumbers);

console.log("The requiredElement");